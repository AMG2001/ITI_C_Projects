#include<stdio.h>
#include<conio.h>

void main(){
	int index=0;
	int numbers[5];
	int input;

	clrscr();

	for(index=0;index<=4;index++){

		printf("Enter the Number : ");

		scanf("%d",&input);

		numbers[index]=input;
	}

	printf("show Array Using for loop : ");

	for(index=0;index<5;index++){
		printf("%d ",numbers[index]);
	}

	printf("\nshow Array using while loop :");

	index = 0;

	while(index<5){
		printf("%d ",numbers[index]);
		index++;

	}
	getch();
}